---
target: index.html
total_score: 30
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 1
target_identity: "file:C:\\Users\\limhe\\OneDrive\\Documents\\GitHub\\macroindicator\\index.html"
target_fingerprint: "sha256:91f902536606541dd618a3bae59dc770096223e1e7932db28e5ed604c56db4a2"
target_path: "C:\\Users\\limhe\\OneDrive\\Documents\\GitHub\\macroindicator\\index.html"
timestamp: 2026-09-18T13-23-51Z
slug: index-html
---
Method: dual-agent (A: general-purpose design-review agent, B: general-purpose detector/browser agent, full completion)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | No per-card stale-data indicator if a series silently fails mid-session |
| 2 | Match System / Real World | 3 | MoM/YoY/MA20/cumret/norm jargon with no inline explanation |
| 3 | User Control and Freedom | 3 | Confirms present but no undo after |
| 4 | Consistency and Standards | 3 | Two visual vocabularies for remove (.ekpi-remove vs .icon-btn) |
| 5 | Error Prevention | 3 | Add Chart/Page Save has no validation feedback |
| 6 | Recognition Rather Than Recall | 3 | Group/Non-group/TH Dashboard split exposes internal data model |
| 7 | Flexibility and Efficiency | 3 | No keyboard shortcuts or bulk operations |
| 8 | Aesthetic and Minimalist Design | 3 | Options panel still mixes theme/font/debug/import |
| 9 | Error Recovery | 3 | Main leak fixed; Import-config alert still leaks raw JS error |
| 10 | Help and Documentation | 3 | No onboarding for the app's three data domains |
| **Total** | | **30/40** | **Good (+4 from 26/40)** |

## Design Specificity Verdict

LLM assessment: genuinely authored for this product - unchanged verdict from prior run.

Deterministic scan: 1 finding remains (down from 27): clipped-overflow-container. Verified NOT a clean false positive - #opt-panel is a real position:absolute element under html's overflow-x:hidden; currently doesn't clip only because it stays within topbar bounds at normal widths, a layout coincidence not a guarantee. Detector's exit code (0) is inconsistent with its own 0=clean/2=findings convention given 1 finding present - flagged as tooling issue.

Visual overlays: browser evidence succeeded - root URL renders app with 0 console errors; /index.html explicit path 404s (server routing quirk). detect.js injection ran, reported no runtime anti-patterns (opt-panel wasn't exercised open). Screenshot capture failed (session tooling timeout), no image evidence this run.

## Overall Impression

All 5 P0-P2 fixes from the prior critique verified as correctly applied, including a properly-done keyboard accessibility retrofit (not cosmetic). One sibling bug surfaced: the "leak backend error" class was fixed on the main load path but missed on the config-import path. Score moved from Acceptable (26/40) to Good (30/40).

## What's Working

- Scoped, non-catastrophic error handling: one series failing only breaks that chart with a targeted retry link
- Keyboard accessibility retrofit done correctly: real role=button tabindex=0 plus onkeydown for Enter/Space
- Data-picker UX reflects genuine data modeling, not an arbitrary UI split

## Priority Issues

**[P1] Import-config error leak reintroduces the original bug class in a sibling path**
Why it matters: alert('Import failed: '+err.message) shows raw JS error text - same failure class as the original Firestore-error finding, fixed on main path but missed here.
Fix: Replace with plain-language Thai messaging, log err to console only.
Suggested command: $impeccable clarify

**[P2] Options panel still mixes unrelated concerns with no grouping**
Fix: Split into Preferences (theme+font) and Data status/Advanced (reference date, export/import).
Suggested command: $impeccable layout

**[P2] Unbounded country-highlight toggling on Inflation page**
Why it matters: groups up to 24 countries with no cap violates the <=4-choice guideline, produces overplotted charts.
Fix: Add a clear affordance, sub-group large regions, or default-limit with explicit show all.
Suggested command: $impeccable distill

**[P3] Function/transform jargon has zero inline help**
Fix: Reuse the existing openInfoPopup() pattern built for Oil Fund projection.
Suggested command: $impeccable clarify

**[P3] No validation feedback on Add Chart/Page Save**
Fix: Disable Save until minimum 1 series + non-empty title.
Suggested command: $impeccable harden

## Persona Red Flags

**Jordan (First-Timer)**: Dashboard tab shows no empty-state copy when 0 cards exist. Group/Non-group/TH Dashboard choice still requires knowing the underlying taxonomy.

**Sam (Accessibility-Dependent)**: Chart.js canvases have no aria-label or data-table fallback. Chart-failure error text has no aria-live region.

**Riley (Stress Tester)**: Delete confirms use synchronous confirm() with no debounce guard. Nested modal z-index stacking untested under rapid open/close.

## Minor Observations

- Chart.js tick labels hardcoded to 10px on canvas, can't be scaled by OS text zoom
- .ekpi-remove hover-only opacity reveal is a mobile-accessibility gap
- alert() for Import failure is also a UX inconsistency versus the rest of the app's in-page styled errors

## Questions to Consider

- Could Group/Non-group/TH Dashboard become a single unified search-first picker?
- Would a soft-delete-with-undo-toast fit the app's recovery-oriented philosophy better than a blocking confirm()?
